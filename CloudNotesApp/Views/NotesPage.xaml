<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CloudNotesApp.Views.NotesPage"
             Title="Notes">
    <ContentPage.Content>
        <StackLayout Padding="10" Spacing="10">
            <SearchBar x:Name="NotesSearchBar" Placeholder="Search notes" TextChanged="OnSearchTextChanged" />
            <CollectionView x:Name="NotesCollectionView" SelectionMode="Single" SelectionChanged="OnNoteSelectionChanged">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" Margin="5" BackgroundColor="LightGray">
                            <Frame.Triggers>
                                <DataTrigger TargetType="Frame" Binding="{Binding IsLocal}" Value="True">
                                    <Setter Property="BackgroundColor" Value="Black" />
                                </DataTrigger>
                                <DataTrigger TargetType="Frame" Binding="{Binding IsLocal}" Value="False">
                                    <Setter Property="BackgroundColor" Value="LightGreen" />
                                </DataTrigger>
                            </Frame.Triggers>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnNoteTapped" CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                            <Label Text="{Binding Content}" LineBreakMode="TailTruncation" />
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" Spacing="20">
                <Button Text="New note" Clicked="OnNewNoteClicked"/>
                <Button Text="Exit" Clicked="OnLogoutClicked"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>